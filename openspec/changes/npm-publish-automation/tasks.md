## 1. Completeness fixes

- [x] 1.1 `fs.cp`/`fsPromises.cp` (used in `packages/cli/src/skillWriter.ts` and `packages/content/scripts/sync-open-design-content.mjs`) needs Node 18+ to be non-experimental; added `"engines": {"node": ">=18"}` to `packages/content`, `packages/mcp-server`, `packages/cli` — matches CI's already-configured `node-version: '20'` in `release.yml` with a safe floor, not a guess
- [x] 1.2 `packages/content/README.md` added
- [x] 1.3 `packages/cli/README.md` added, matching the root README's `npx @feimacode/open-design-agent-kit init` bullet with `--tools` usage and a per-host breakdown of what gets written

## 2. Version pinning

- [x] 2.1 `scripts/pin-content-version-for-publish.mjs`: rewrites `packages/mcp-server/package.json`'s `@feimacode/open-design-agent-kit-content` dependency from `*` to `^<content's current version>`, reading the version straight from `packages/content/package.json` rather than taking it as an argument (one less thing for the calling workflow to get wrong)
- [x] 2.2 Tested locally for real: ran the script (mutating the real working tree, since there's no other way to test what `npm pack` would actually embed), confirmed via `npm pack` + extracting the real tarball that the packed `package.json` contains `"^0.1.0"`, then reverted the working-tree change back to `"*"` (confirmed via `git diff` showing only the intended `engines` addition remaining)

## 3. Automated publish smoke test

- [x] 3.1 `scripts/test-npm-publish.mjs`: starts local Verdaccio with a temp config scoping `@feimacode/*` to open publish (`max_users: 10000`, not `-1` — verdaccio 6.x rejects `-1` as "user registration disabled" despite older docs suggesting it means unlimited, found by hand and fixed then, now baked into this script), registers a throwaway user via the registry's own HTTP API, `npm publish`s all three packages in order
- [x] 3.2 Same script: from an isolated fresh directory (own `.npmrc`, `npm_config_registry` env override, no access to the monorepo's node_modules) — `npx --yes @feimacode/open-design-agent-kit-mcp`, drives a real JSON-RPC handshake, asserts exactly 9 tools and a non-empty result from `list_open_design_skills`
- [x] 3.3 Same script: `npx --yes @feimacode/open-design-agent-kit init . --tools all` against a second fresh scratch dir, asserts 24 skills per host, `.mcp.json`'s `open-design` entry, and `.codex/config.toml`'s existence
- [x] 3.4 Ran the script for real, twice. **Real bug found and fixed on the first run**: it printed `ALL CHECKS PASSED` and returned exit 0, but `ps aux` afterward showed the local `verdaccio` process (and, separately, the test-spawned `@feimacode/open-design-agent-kit-mcp` server) both still running — `child.kill()` only signals the direct `npx` process, not the actual binary `npx` spawns as a grandchild, so the real process survived its own parent's death. Fixed by spawning both with `detached: true` and killing the whole process group (`process.kill(-child.pid, 'SIGTERM')`) instead of just the direct child. Re-ran twice more and confirmed via `ps aux` both times that nothing was left running and no `od-registry-`/`od-npmrc-`/`od-fresh-mcp-`/`od-fresh-cli-` temp directories survived either — verified the fix, not just assumed it from reading the diff.

## 4. CI workflows — one shared release, per the user's explicit correction to this plan

- [x] 4.1 Extended `.github/workflows/release.yml`'s `build` job: after the existing typecheck/lint/test/VSIX-package steps, added steps that run `scripts/pin-content-version-for-publish.mjs`, build `mcp-server`+`cli`, run `scripts/test-npm-publish.mjs` as a hard gate, then `npm pack` all three. **Also added a task not originally listed**: a "Validate npm package versions match the release" step in the `validate` job, mirroring the existing `packages/vscode/package.json` version-match check — the "one shared release" guarantee is meaningless if the three npm packages' own versions could silently drift from the tag/vscode version with nothing to catch it.
- [x] 4.2 Extended `release.yml`'s `release` job: checksums generated for each `.tgz` (`for f in *.tgz; do sha256sum "$f" > "${f}.sha256"; done`), renamed the upload/download artifact bundle from `vsix` to `release-artifacts` (it's no longer vsix-only) with `release/*.tgz` added to both its upload path and the `Create GitHub Release` step's `files:` list, and updated the release summary to list all four artifacts
- [x] 4.3 New `.github/workflows/publish-npm.yml`, mirroring `publish-marketplace.yml`'s shape exactly: `workflow_dispatch`, typed `"PUBLISH"` confirmation, `NPM_TOKEN` secret check, verifies all three tarball assets exist on the release before downloading, `gh release download`s + verifies checksums, then `npm publish <tarball>` for content → mcp-server → cli in that order (comment explains why that order, not just what) — no rebuilding. Uses `actions/setup-node@v4`'s `registry-url` + `NODE_AUTH_TOKEN` env, the standard GitHub Actions npm-publish pattern. Deviation from the plan's literal wording: allows npm prerelease versions (`-alpha.N`/`-beta.N`), unlike `publish-marketplace.yml`'s stricter plain-semver-only rule — correct because npm, unlike the VS Code Marketplace, natively supports prerelease versions.
- [x] 4.4 Verified both workflow files for real: valid YAML (`python3 -c "import yaml; yaml.safe_load(...)"`), and — a real, authoritative check, not just YAML syntax — ran the actual `actionlint` tool (the standard GitHub Actions workflow linter, checks expression syntax, job/output references, and shellcheck-style issues inside `run:` blocks) via its official Docker image against all three workflow files (including the untouched `publish-marketplace.yml`, as a control): `0 errors in 2 files` (and clean against all three together). Also verified the exact `npm pack --workspace=packages/<name> --pack-destination <dir>` invocations used in the workflow actually work, by running them for real from the repo root — all three tarballs produced with correct names and file counts. Full real-world verification (an actual GitHub Actions run, needing `NPM_TOKEN`/`GITHUB_TOKEN` and real network/registry access from within GitHub's infrastructure) is out of reach from this environment and tracked honestly as unverified until run for real, matching this session's established pattern.
