{
  "name": "@feimacode/open-design-agent-kit-core",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic content parsing and generation-instruction logic for Open Design Agent Kit, shared by the VS Code extension and (later) other agent-host packages.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "dependencies": {
    "gray-matter": "^4.0.3",
    "pdf-lib": "^1.17.1",
    "pptxgenjs": "^4.0.1",
    "puppeteer-core": "^24.43.1"
  },
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint src --ext ts",
    "test:unit": "rm -rf out && tsc -p . --outDir out --noEmit false && mocha 'out/test/unit/**/*.test.js' --timeout 5000"
  }
}
